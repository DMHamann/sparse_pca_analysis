[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "sparse_pca_analysis"
version = "0.1.0"
description = ""
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
